import React from "react"
import { Text, View } from "react-native"
import { styles } from "../theme/appTheme"


export const SettingsScreen = () => {
    return (
        <View style={ styles.globalMargin }>
            <Text style={ styles.title }>Settings Screen :D</Text>
        </View>
    )
}